# WaterByHoor — دليل الاستخدام الكامل

## ملاحظة مهمة قبل كل شيء

بيئة العمل التي أنشئ فيها هذا المشروع **لا تملك اتصال إنترنت ولا Android SDK/Gradle مثبّتين**،
لذلك لا أستطيع بناء ملف `.apk` فعليًا بنفسي هنا، ولا تشغيل الاختبارات الـ16 المطلوبة على جهاز حقيقي.
ما فعلته بدلاً من ذلك: كتبت المشروع كاملاً (كل كود Kotlin وملفات الموارد وGradle) بعناية،
مع التحقق من كل واجهة برمجية (API) من Jetpack Glance عبر التوثيق الرسمي أثناء الكتابة لتقليل احتمال الأخطاء —
لكن هذا لا يغني عن بناء حقيقي على جهاز يملك اتصال إنترنت.

لحل مشكلة "أريد تثبيت APK على جوالي بدون Android Studio"، أضفت **GitHub Actions**:
خط بناء آلي يعمل على خوادم GitHub (التي تملك إنترنت كامل)، يبني الـAPK وينزّله لك جاهزًا كرابط تفتحه من الجوال مباشرة.
هذا يحقق ما طلبتيه فعليًا: **لا تحتاجين Android Studio إطلاقًا**، فقط متصفح وحساب GitHub مجاني.

---

## 1) قائمة الملفات ووظيفة كل ملف

```
WaterByHoor/
├── settings.gradle.kts              إعداد المشروع والمستودعات (google/mavenCentral)
├── build.gradle.kts                 إصدارات Android Gradle Plugin وKotlin
├── gradle.properties                إعدادات Gradle العامة (AndroidX مفعّل...)
├── .github/workflows/build-apk.yml  خط بناء آلي يبني الـAPK على خوادم GitHub
│
└── app/
    ├── build.gradle.kts             تبعيات التطبيق (Compose, Glance) وminSdk/targetSdk
    ├── proguard-rules.pro           فارغ تقريبًا (لا تصغير كود مفعّل)
    │
    └── src/main/
        ├── AndroidManifest.xml      تعريف التطبيق، الـActivity، والـWidget Receiver
        │                            (بدون أي صلاحية إنترنت)
        │
        ├── java/com/hoor/waterbyhoor/
        │   ├── WaterRepository.kt   "مصدر الحقيقة الوحيد" للبيانات (SharedPreferences)
        │   │                        يخزّن: dailyWater, dailyGoal, selectedAmount, lastUpdatedDate
        │   ├── DateUtils.kt         حساب "اليوم المنطقي" الذي يبدأ الساعة 5 صباحًا
        │   ├── WhatsAppHelper.kt    بناء رسالة واتساب وفتحها عبر wa.me
        │   ├── MainActivity.kt      شاشة التطبيق الرئيسي (Compose): عرض اليوم + تعديل
        │   │                        الهدف وكمية الإضافة الافتراضية عبر أزرار [-] [+]
        │   │
        │   └── widget/
        │       ├── WaterWidget.kt           تصميم الـWidget بالكامل (Jetpack Glance)
        │       ├── WaterWidgetReceiver.kt   نقطة الاتصال بين نظام Android والـWidget
        │       └── actions/
        │           ├── AddWaterAction.kt        زر "+ إضافة": يحدّث البيانات محليًا فقط
        │           ├── SelectAmountAction.kt     اختيار كمية من الأزرار الجاهزة
        │           └── SendWhatsAppAction.kt     زر "أرسل للمدربة": يبني الرسالة ويفتح واتساب
        │
        └── res/
            ├── xml/water_widget_info.xml       إعدادات الـWidget (الحجم، إمكانية تغيير الحجم...)
            ├── layout/widget_loading.xml        شاشة تحميل بسيطة تظهر لحظة إضافة الـWidget
            ├── values/ و values-night/          ألوان ونصوص، مع دعم تلقائي للـDark Mode
            ├── drawable/                        أيقونة التطبيق (قطرة ماء، مرسومة كـVector بدون صور خارجية)
            └── mipmap-anydpi-v26/                أيقونة adaptive icon
```

---

## 2) كيفية الحصول على APK جاهز للتثبيت — بدون Android Studio إطلاقًا

الخطوات التالية تُنفَّذ من متصفح الجوال مباشرة:

1. **أنشئي حساب GitHub** مجاني إن لم يكن لديك (github.com).
2. من الجوال، اذهبي إلى github.com واضغطي **New repository**، سمّيه مثلاً `WaterByHoor`، اجعليه Public، ثم أنشئيه.
3. داخل الريبو الجديد اضغطي **Add file → Upload files**، وارفعي كل ملفات المشروع
   (يمكنك ضغط المجلد كله وفك ضغطه هنا محليًا ثم سحب الملفات، أو رفعها مجلدًا مجلدًا —
   تأكدي أن البنية (المسارات) تبقى كما هي تمامًا، خصوصًا مجلد `.github/workflows/`).
4. بعد الرفع مباشرة، سيبدأ GitHub Actions بالعمل تلقائيًا. لمتابعته: افتحي تبويب **Actions** أعلى الريبو.
5. انتظري حتى تكتمل العملية (علامة ✅ خضراء)، عادة تأخذ بضع دقائق.
6. اذهبي إلى تبويب **Releases** في الريبو (أو صفحة الـActions نفسها → قسم Artifacts).
   ستجدين ملف APK جاهزًا للتنزيل باسم قريب من `app-debug.apk`.
7. نزّلي الملف من الجوال مباشرة، ثم افتحيه لتثبيته.
   في أول مرة سيطلب منك Android تفعيل **"السماح بالتثبيت من هذا المصدر"** (المتصفح) — فعّليها ثم أكملي التثبيت.

هذا الـAPK "debug" (موقّع بتوقيع تجريبي تلقائي) — وهذا طبيعي وكافٍ تمامًا للاستخدام الشخصي،
ولا يحتاج أي إعداد شهادات إضافي منك.

> بديل: إذا كان لديك حاسوب مع Android Studio، يمكنك فتح المشروع فيه مباشرة وسيقوم تلقائيًا
> بتنزيل Gradle Wrapper وكل التبعيات، ثم Run على جهازك أو محاكي — لكن هذا غير ضروري إطلاقًا
> بعد إضافة GitHub Actions أعلاه.

---

## 3) كيفية إضافة الـWidget إلى الشاشة الرئيسية

1. بعد تثبيت التطبيق، اضغطي مطولًا على مكان فارغ في الشاشة الرئيسية.
2. اختاري **Widgets**.
3. ابحثي عن **WaterByHoor** في القائمة.
4. اسحبي الـWidget إلى الشاشة الرئيسية. يمكنك تغيير حجمه (تكبير/تصغير) حسب رغبتك.

---

## 4) القيود التقنية — خصوصًا موضوع الـSlider

فحصت إمكانيات Jetpack Glance الحالية: **الـSlider التفاعلي غير مدعوم داخل Home Screen Widgets**
(الـWidgets مبنية فعليًا فوق `RemoteViews`، وGlance لا يوفر لها مكوّن Slider قابل للسحب المباشر مثل
الموجود في تطبيق عادي بـCompose).

البديل الذي استخدمته (وهو الأنسب للـUX داخل Widget، أسرع وأكثر موثوقية من أي "حل بديل" معقّد):
**شبكة من الأزرار الجاهزة** [10] [50] [100] [200] [250] [300] [330]،
يظهر الزر المختار حاليًا بلون مختلف (Highlight)، والاختيار يتم بضغطة واحدة فقط ويحفظ فورًا.
هذا يحقق نفس الهدف العملي للـSlider (اختيار كمية بسرعة) دون الاعتماد على مكوّن غير مدعوم رسميًا.

قيود تقنية أخرى مهمة اتبعتها بدقة حسب طلبك:
- **بدون أي صلاحية إنترنت** في AndroidManifest — فتح واتساب يتم عبر Intent فقط (`https://wa.me/...`)،
  والجهة التي تقوم بأي اتصال شبكة هي واتساب أو المتصفح، وليس تطبيقك.
- **بدون خدمة خلفية دائمة (Service) وبدون Polling**: تحديث الـWidget يحدث فقط عند تفاعل المستخدم
  (إضافة ماء، تغيير الهدف...)، عبر الطريقة الرسمية `GlanceAppWidget().update(...)` / `updateAll(...)`.
- **Daily Reset بدون فتح التطبيق**: كل مرة يُرسم فيها الـWidget، يتحقق الكود من التاريخ الحالي
  (باعتبار أن اليوم يبدأ الساعة 5 صباحًا) ويصفّر كمية اليوم تلقائيًا إذا تغيّر اليوم — هذا منطق يعمل
  في `WaterRepository.ensureFreshDay()` ويُستدعى في كل قراءة للبيانات.
- **مصدر بيانات واحد** (`SharedPreferences` باسم `water_prefs`) يقرأ ويكتب منه كل من التطبيق
  الرئيسي والـWidget، فلا يوجد أي تعارض بين الاثنين.

---

## 5) قائمة الاختبار (16 بندًا) — تحتاجين تنفيذها بنفسك بعد التثبيت

لأنني لا أملك جهاز Android حقيقي أو محاكي هنا لأشغّل عليه المشروع فعليًا، هذه القائمة **لم تُنفَّذ فعليًا مني**؛
إليك ما أتوقعه بناءً على الكود المكتوب، لكن يُرجى التحقق منها بنفسك بعد التثبيت (تأخذ دقائق قليلة):

1. البناء بدون أخطاء ← ستتأكدين من هذا عمليًا عند نجاح GitHub Actions (علامة ✅).
2. فتح التطبيق بدون Crash.
3. ظهور الـWidget في قائمة الـWidgets.
4. وضع الـWidget على الشاشة الرئيسية.
5. عرض كمية الماء بشكل صحيح.
6. إضافة الماء من الـWidget دون فتح التطبيق.
7. تحديث الـWidget بعد الإضافة.
8. تغيير الهدف من التطبيق الرئيسي ينعكس على الـWidget.
9. تغيير كمية الإضافة الافتراضية ينعكس على الـWidget.
10. الـDaily Reset عند تغيّر اليوم (الساعة 5 صباحًا).
11. عدم فقدان البيانات عند إغلاق التطبيق (البيانات في SharedPreferences دائمة).
12. عمل زر واتساب وفتح الرسالة جاهزة.
13. عدم حدوث Crash إن لم يكن واتساب مثبتًا (تم التعامل معه بـ try/catch + رسالة Toast).
14. عمل Light/Dark Mode (الألوان تعتمد على GlanceTheme + موارد values-night).
15. عدم وجود أي طلبات شبكة من التطبيق نفسه (لا صلاحية إنترنت في Manifest أصلاً).
16. عدم وجود إعلانات أو Analytics أو Tracking (لم تُضَف أي مكتبة من هذا النوع).

إذا واجهتِ أي خطأ بناء (Build error) تحديدًا، أرسليه لي بنصه الكامل وسأصلحه فورًا.
